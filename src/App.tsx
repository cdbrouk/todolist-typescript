import React from 'react';

const App = () => {
  return (
    <div className="App">
      React Typescript + eslint airbnb, prettier + Redux + Styled-Components
    </div>
  );
};

export default App;
